package com.neusoft.neusoft_project.service;

import com.baomidou.mybatisplus.extension.service.IService;
import com.neusoft.neusoft_project.entity.SystemLog;

public interface ISystemLogService extends IService<SystemLog> {
    // IService gives you .save(), .list(), .listByQuery() etc. for free
    // No extra methods needed for now
}